import { createFileRoute } from '@tanstack/react-router'
import { useState } from 'react'
export const Route = createFileRoute('/contact')({ component: Contact })
function Contact(){const [sent,setSent]=useState(false); const [error,setError]=useState(''); function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget); if(!f.get('name')||!f.get('email')||!f.get('message')){setError('Merci de remplir les champs indiqués.');return} setError('');setSent(true)} return <section className="page section contact-page"><div><p className="eyebrow">Contact</p><h1>Faisons connaissance.</h1><p className="page-intro">La séance découverte est offerte et sans engagement. Écrivez-moi pour trouver le créneau qui vous convient.</p><div className="contact-details"><p><strong>Le cabinet</strong><br />1 rue de la Halbarderie<br />44470 Thouaré-sur-Loire</p><p><strong>Horaires</strong><br />Lundi et vendredi · 8h – 19h30<br />Mercredi et samedi matin · 8h – 13h30</p></div></div><form className="contact-form" onSubmit={submit} noValidate><label>Nom et prénom *<input name="name" required /></label><label>Adresse e-mail *<input name="email" type="email" required /></label><label>Votre message *<textarea name="message" rows={5} required /></label>{error&&<p className="form-error">{error}</p>}<button className="button" type="submit">Envoyer ma demande</button><p className="form-note">Formulaire de démonstration locale — aucun message n’est envoyé.</p><p className="sr-only" aria-live="polite">{sent?'Votre demande a bien été enregistrée. Je reviendrai vers vous rapidement.':''}</p>{sent&&<div className="success" role="status">Merci, votre demande a bien été enregistrée.<br />Je reviendrai vers vous rapidement.</div>}</form></section>}
